import cv2
import pydicom
import sys
import numpy as np

#while(count<15):
#    img1 = cv2.imread(r'C:\Users\trainee\Desktop\productivity.jpg')
  # img2 = cv2.imread(r'C:\Users\trainee\Desktop\productivity%s.jpg'%count)
   # img_rotate_90_clockwise = cv2.rotate(r'C:\Users\trainee\Desktop\productivity.jpg', cv2.ROTATE_90_COUNTERCLOCKWISE)
    #vis = np.column_stack((img1,img2))
    #vis = np.concatenate((img1, img2), axis=0)
    #img_rotate_90_clockwise = cv2.rotate(vis, cv2.ROTATE_90_CLOCKWISE)
    #cv2.imwrite(r'C:\Users\trainee\Desktop\productivity.jpg', img_rotate_90_clockwise)
    #if count/3 == 0:
     #       img1 = cv2.imread(r'C:\Users\trainee\Desktop\productivity.jpg')
      #      img2 = cv2.imread(r'C:\Users\trainee\Desktop\productivity%s.jpg'%count)
       #     vis = np.column_stack((img1,img2))
            #vis = np.concatenate((img1, img2), axis=1)
        #    cv2.imwrite(r'C:\Users\trainee\Desktop\productivity.jpg', vis)

#img1 = cv2.imread(r'C:\Users\trainee\Desktop\download(1).jpg')

#img2 = cv2.imread(r'C:\Users\trainee\Desktop\productivity13_.jpg')
    #vis = np.column_stack((img1,img2))
#vis = np.concatenate((img1, img2), axis=1)
#print("hai")
#cv2.imwrite(r'C:\Users\trainee\Desktop\productivity0.jpg', vis)


#cv2.destroyAllWindows()
# Load two images
img1 = cv2.imread(r'C:\Users\trainee\Desktop\download(1).jpg')
img2 = cv2.imread(r'C:\Users\trainee\Desktop\productivity13_.jpg')
# I want to put logo on top-left corner, So I create a ROI
rows,cols,channels = img2.shape
roi = img1[0:rows, 0:cols]
# Now create a mask of logo and create its inverse mask also
img2gray = cv2.cvtColor(img2,cv2.COLOR_BGR2GRAY)
ret, mask = cv2.threshold(img2gray, 10, 255, cv2.THRESH_BINARY)
mask_inv = cv2.bitwise_not(mask)
# Now black-out the area of logo in ROI
img1_bg = cv2.bitwise_and(roi,roi,mask = mask_inv)
# Take only region of logo from logo image.
img2_fg = cv2.bitwise_and(img2,img2,mask = mask)
# Put logo in ROI and modify the main image
dst = cv2.add(img1_bg,img2_fg)
img1[0:rows, 0:cols ] = dst
cv2.imshow('res',img1)
cv2.waitKey(0)
cv2.destroyAllWindows()
