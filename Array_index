#Create an index of arrays
inside = []
holder = []
container = []

holder.append([])
container += holder

value=[1,2,3,4,5]

for i in range(0,4):
    
    inside = value[i]
    map(inside,holder[i])
    holder.append([inside])

print(holder)
